<!-- GENERATED into the public mirror by scripts/export-public.sh from
     docs/public-github/ in the private monorepo. Edit it HERE. -->

## What this changes

<!-- What it does, and why. Link the issue it fixes: "Fixes #123". -->

## How it was tested

<!-- The commands you ran, the platform you ran them on. For a bug fix: how you
     confirmed the new test fails without the fix. -->

## Checklist

- [ ] Every commit is signed off (`git commit -s`) — [DCO](../DCO)
- [ ] `make build` and `make verify` pass
- [ ] `go test ./...` passes in each module I touched
- [ ] A bug fix comes with a test that fails without it
- [ ] The docs in this repository still match the code

<!-- Accepted pull requests are applied upstream with you as the author, then
     arrive here in the next sync, and this pull request is closed with a link
     to that commit. It is not a rejection — see CONTRIBUTING.md. -->

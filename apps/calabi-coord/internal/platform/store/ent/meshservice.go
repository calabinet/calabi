// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/calabi/calabi/apps/calabi-coord/internal/platform/store/ent/meshservice"
)

// MeshService is the model entity for the MeshService schema.
type MeshService struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// owning meshnet == org id
	MeshnetID int64 `json:"meshnet_id,omitempty"`
	// the node offering it
	NodeID int64 `json:"node_id,omitempty"`
	// service label; an ACL selector, unique per node but shared across nodes on purpose (one service, many instances)
	Name string `json:"name,omitempty"`
	// tcp | udp
	Proto string `json:"proto,omitempty"`
	// Port holds the value of the "port" field.
	Port int `json:"port,omitempty"`
	// what the DEVICE dials to reach the app (host:port); empty = 127.0.0.1:<port>. Distinct from port, which is what mesh peers dial on the overlay address — an app bound to loopback answers the first and not the second
	Target string `json:"target,omitempty"`
	// free-text note for humans
	Note string `json:"note,omitempty"`
	// node = the machine's config declares it (a claim); console = an admin entered it (an authorization). Defaults to node so every pre-existing row keeps being reconciled against its device
	Source string `json:"source,omitempty"`
	// gates ACL visibility: a node's claim matches no svc: rule until an admin confirms it. Defaults TRUE so services that predate this never silently stop matching
	Approved bool `json:"approved,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MeshService) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case meshservice.FieldApproved:
			values[i] = new(sql.NullBool)
		case meshservice.FieldID, meshservice.FieldMeshnetID, meshservice.FieldNodeID, meshservice.FieldPort:
			values[i] = new(sql.NullInt64)
		case meshservice.FieldName, meshservice.FieldProto, meshservice.FieldTarget, meshservice.FieldNote, meshservice.FieldSource:
			values[i] = new(sql.NullString)
		case meshservice.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MeshService fields.
func (_m *MeshService) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case meshservice.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case meshservice.FieldMeshnetID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field meshnet_id", values[i])
			} else if value.Valid {
				_m.MeshnetID = value.Int64
			}
		case meshservice.FieldNodeID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field node_id", values[i])
			} else if value.Valid {
				_m.NodeID = value.Int64
			}
		case meshservice.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case meshservice.FieldProto:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field proto", values[i])
			} else if value.Valid {
				_m.Proto = value.String
			}
		case meshservice.FieldPort:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field port", values[i])
			} else if value.Valid {
				_m.Port = int(value.Int64)
			}
		case meshservice.FieldTarget:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field target", values[i])
			} else if value.Valid {
				_m.Target = value.String
			}
		case meshservice.FieldNote:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field note", values[i])
			} else if value.Valid {
				_m.Note = value.String
			}
		case meshservice.FieldSource:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field source", values[i])
			} else if value.Valid {
				_m.Source = value.String
			}
		case meshservice.FieldApproved:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field approved", values[i])
			} else if value.Valid {
				_m.Approved = value.Bool
			}
		case meshservice.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MeshService.
// This includes values selected through modifiers, order, etc.
func (_m *MeshService) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this MeshService.
// Note that you need to call MeshService.Unwrap() before calling this method if this MeshService
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *MeshService) Update() *MeshServiceUpdateOne {
	return NewMeshServiceClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the MeshService entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *MeshService) Unwrap() *MeshService {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: MeshService is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *MeshService) String() string {
	var builder strings.Builder
	builder.WriteString("MeshService(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("meshnet_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.MeshnetID))
	builder.WriteString(", ")
	builder.WriteString("node_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.NodeID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("proto=")
	builder.WriteString(_m.Proto)
	builder.WriteString(", ")
	builder.WriteString("port=")
	builder.WriteString(fmt.Sprintf("%v", _m.Port))
	builder.WriteString(", ")
	builder.WriteString("target=")
	builder.WriteString(_m.Target)
	builder.WriteString(", ")
	builder.WriteString("note=")
	builder.WriteString(_m.Note)
	builder.WriteString(", ")
	builder.WriteString("source=")
	builder.WriteString(_m.Source)
	builder.WriteString(", ")
	builder.WriteString("approved=")
	builder.WriteString(fmt.Sprintf("%v", _m.Approved))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// MeshServices is a parsable slice of MeshService.
type MeshServices []*MeshService

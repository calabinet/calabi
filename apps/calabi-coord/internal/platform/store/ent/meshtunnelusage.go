// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/calabinet/calabi/apps/calabi-coord/internal/platform/store/ent/meshtunnelusage"
)

// MeshTunnelUsage is the model entity for the MeshTunnelUsage schema.
type MeshTunnelUsage struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// MeshnetID holds the value of the "meshnet_id" field.
	MeshnetID int64 `json:"meshnet_id,omitempty"`
	// NodeID holds the value of the "node_id" field.
	NodeID int64 `json:"node_id,omitempty"`
	// the tunnel's name on the node
	Name string `json:"name,omitempty"`
	// UTC hour, truncated
	Hour time.Time `json:"hour,omitempty"`
	// visitors to the local service
	BytesIn int64 `json:"bytes_in,omitempty"`
	// the local service back to visitors
	BytesOut     int64 `json:"bytes_out,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MeshTunnelUsage) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case meshtunnelusage.FieldID, meshtunnelusage.FieldMeshnetID, meshtunnelusage.FieldNodeID, meshtunnelusage.FieldBytesIn, meshtunnelusage.FieldBytesOut:
			values[i] = new(sql.NullInt64)
		case meshtunnelusage.FieldName:
			values[i] = new(sql.NullString)
		case meshtunnelusage.FieldHour:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MeshTunnelUsage fields.
func (mtu *MeshTunnelUsage) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case meshtunnelusage.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			mtu.ID = int(value.Int64)
		case meshtunnelusage.FieldMeshnetID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field meshnet_id", values[i])
			} else if value.Valid {
				mtu.MeshnetID = value.Int64
			}
		case meshtunnelusage.FieldNodeID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field node_id", values[i])
			} else if value.Valid {
				mtu.NodeID = value.Int64
			}
		case meshtunnelusage.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				mtu.Name = value.String
			}
		case meshtunnelusage.FieldHour:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field hour", values[i])
			} else if value.Valid {
				mtu.Hour = value.Time
			}
		case meshtunnelusage.FieldBytesIn:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field bytes_in", values[i])
			} else if value.Valid {
				mtu.BytesIn = value.Int64
			}
		case meshtunnelusage.FieldBytesOut:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field bytes_out", values[i])
			} else if value.Valid {
				mtu.BytesOut = value.Int64
			}
		default:
			mtu.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MeshTunnelUsage.
// This includes values selected through modifiers, order, etc.
func (mtu *MeshTunnelUsage) Value(name string) (ent.Value, error) {
	return mtu.selectValues.Get(name)
}

// Update returns a builder for updating this MeshTunnelUsage.
// Note that you need to call MeshTunnelUsage.Unwrap() before calling this method if this MeshTunnelUsage
// was returned from a transaction, and the transaction was committed or rolled back.
func (mtu *MeshTunnelUsage) Update() *MeshTunnelUsageUpdateOne {
	return NewMeshTunnelUsageClient(mtu.config).UpdateOne(mtu)
}

// Unwrap unwraps the MeshTunnelUsage entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (mtu *MeshTunnelUsage) Unwrap() *MeshTunnelUsage {
	_tx, ok := mtu.config.driver.(*txDriver)
	if !ok {
		panic("ent: MeshTunnelUsage is not a transactional entity")
	}
	mtu.config.driver = _tx.drv
	return mtu
}

// String implements the fmt.Stringer.
func (mtu *MeshTunnelUsage) String() string {
	var builder strings.Builder
	builder.WriteString("MeshTunnelUsage(")
	builder.WriteString(fmt.Sprintf("id=%v, ", mtu.ID))
	builder.WriteString("meshnet_id=")
	builder.WriteString(fmt.Sprintf("%v", mtu.MeshnetID))
	builder.WriteString(", ")
	builder.WriteString("node_id=")
	builder.WriteString(fmt.Sprintf("%v", mtu.NodeID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(mtu.Name)
	builder.WriteString(", ")
	builder.WriteString("hour=")
	builder.WriteString(mtu.Hour.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("bytes_in=")
	builder.WriteString(fmt.Sprintf("%v", mtu.BytesIn))
	builder.WriteString(", ")
	builder.WriteString("bytes_out=")
	builder.WriteString(fmt.Sprintf("%v", mtu.BytesOut))
	builder.WriteByte(')')
	return builder.String()
}

// MeshTunnelUsages is a parsable slice of MeshTunnelUsage.
type MeshTunnelUsages []*MeshTunnelUsage

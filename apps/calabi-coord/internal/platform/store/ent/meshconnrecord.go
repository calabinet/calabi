// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/calabi/calabi/apps/calabi-coord/internal/platform/store/ent/meshconnrecord"
)

// MeshConnRecord is the model entity for the MeshConnRecord schema.
type MeshConnRecord struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// owning meshnet == org id; every read is scoped to it
	MeshnetID int64 `json:"meshnet_id,omitempty"`
	// the device that REPORTED this row (and sent the bytes_tx)
	SrcNodeID int64 `json:"src_node_id,omitempty"`
	// the peer it exchanged traffic with, resolved from the reported node key WITHIN the same meshnet — a key from another org resolves to nothing and the sample is dropped
	DstNodeID int64 `json:"dst_node_id,omitempty"`
	// UTC hour this traffic fell in, truncated; the bucket key
	Hour time.Time `json:"hour,omitempty"`
	// bytes src sent to dst during the hour, summed from the node's reports
	BytesTx int64 `json:"bytes_tx,omitempty"`
	// bytes src received from dst during the hour
	BytesRx int64 `json:"bytes_rx,omitempty"`
	// "direct" or "relay" as last reported in the hour. NOT an endpoint address — see the type comment
	Path string `json:"path,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MeshConnRecord) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case meshconnrecord.FieldID, meshconnrecord.FieldMeshnetID, meshconnrecord.FieldSrcNodeID, meshconnrecord.FieldDstNodeID, meshconnrecord.FieldBytesTx, meshconnrecord.FieldBytesRx:
			values[i] = new(sql.NullInt64)
		case meshconnrecord.FieldPath:
			values[i] = new(sql.NullString)
		case meshconnrecord.FieldHour, meshconnrecord.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MeshConnRecord fields.
func (mcr *MeshConnRecord) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case meshconnrecord.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			mcr.ID = int(value.Int64)
		case meshconnrecord.FieldMeshnetID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field meshnet_id", values[i])
			} else if value.Valid {
				mcr.MeshnetID = value.Int64
			}
		case meshconnrecord.FieldSrcNodeID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field src_node_id", values[i])
			} else if value.Valid {
				mcr.SrcNodeID = value.Int64
			}
		case meshconnrecord.FieldDstNodeID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field dst_node_id", values[i])
			} else if value.Valid {
				mcr.DstNodeID = value.Int64
			}
		case meshconnrecord.FieldHour:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field hour", values[i])
			} else if value.Valid {
				mcr.Hour = value.Time
			}
		case meshconnrecord.FieldBytesTx:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field bytes_tx", values[i])
			} else if value.Valid {
				mcr.BytesTx = value.Int64
			}
		case meshconnrecord.FieldBytesRx:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field bytes_rx", values[i])
			} else if value.Valid {
				mcr.BytesRx = value.Int64
			}
		case meshconnrecord.FieldPath:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field path", values[i])
			} else if value.Valid {
				mcr.Path = value.String
			}
		case meshconnrecord.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				mcr.UpdatedAt = value.Time
			}
		default:
			mcr.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MeshConnRecord.
// This includes values selected through modifiers, order, etc.
func (mcr *MeshConnRecord) Value(name string) (ent.Value, error) {
	return mcr.selectValues.Get(name)
}

// Update returns a builder for updating this MeshConnRecord.
// Note that you need to call MeshConnRecord.Unwrap() before calling this method if this MeshConnRecord
// was returned from a transaction, and the transaction was committed or rolled back.
func (mcr *MeshConnRecord) Update() *MeshConnRecordUpdateOne {
	return NewMeshConnRecordClient(mcr.config).UpdateOne(mcr)
}

// Unwrap unwraps the MeshConnRecord entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (mcr *MeshConnRecord) Unwrap() *MeshConnRecord {
	_tx, ok := mcr.config.driver.(*txDriver)
	if !ok {
		panic("ent: MeshConnRecord is not a transactional entity")
	}
	mcr.config.driver = _tx.drv
	return mcr
}

// String implements the fmt.Stringer.
func (mcr *MeshConnRecord) String() string {
	var builder strings.Builder
	builder.WriteString("MeshConnRecord(")
	builder.WriteString(fmt.Sprintf("id=%v, ", mcr.ID))
	builder.WriteString("meshnet_id=")
	builder.WriteString(fmt.Sprintf("%v", mcr.MeshnetID))
	builder.WriteString(", ")
	builder.WriteString("src_node_id=")
	builder.WriteString(fmt.Sprintf("%v", mcr.SrcNodeID))
	builder.WriteString(", ")
	builder.WriteString("dst_node_id=")
	builder.WriteString(fmt.Sprintf("%v", mcr.DstNodeID))
	builder.WriteString(", ")
	builder.WriteString("hour=")
	builder.WriteString(mcr.Hour.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("bytes_tx=")
	builder.WriteString(fmt.Sprintf("%v", mcr.BytesTx))
	builder.WriteString(", ")
	builder.WriteString("bytes_rx=")
	builder.WriteString(fmt.Sprintf("%v", mcr.BytesRx))
	builder.WriteString(", ")
	builder.WriteString("path=")
	builder.WriteString(mcr.Path)
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(mcr.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// MeshConnRecords is a parsable slice of MeshConnRecord.
type MeshConnRecords []*MeshConnRecord

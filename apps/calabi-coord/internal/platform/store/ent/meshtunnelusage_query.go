// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/calabinet/calabi/apps/calabi-coord/internal/platform/store/ent/meshtunnelusage"
	"github.com/calabinet/calabi/apps/calabi-coord/internal/platform/store/ent/predicate"
)

// MeshTunnelUsageQuery is the builder for querying MeshTunnelUsage entities.
type MeshTunnelUsageQuery struct {
	config
	ctx        *QueryContext
	order      []meshtunnelusage.OrderOption
	inters     []Interceptor
	predicates []predicate.MeshTunnelUsage
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MeshTunnelUsageQuery builder.
func (mtuq *MeshTunnelUsageQuery) Where(ps ...predicate.MeshTunnelUsage) *MeshTunnelUsageQuery {
	mtuq.predicates = append(mtuq.predicates, ps...)
	return mtuq
}

// Limit the number of records to be returned by this query.
func (mtuq *MeshTunnelUsageQuery) Limit(limit int) *MeshTunnelUsageQuery {
	mtuq.ctx.Limit = &limit
	return mtuq
}

// Offset to start from.
func (mtuq *MeshTunnelUsageQuery) Offset(offset int) *MeshTunnelUsageQuery {
	mtuq.ctx.Offset = &offset
	return mtuq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mtuq *MeshTunnelUsageQuery) Unique(unique bool) *MeshTunnelUsageQuery {
	mtuq.ctx.Unique = &unique
	return mtuq
}

// Order specifies how the records should be ordered.
func (mtuq *MeshTunnelUsageQuery) Order(o ...meshtunnelusage.OrderOption) *MeshTunnelUsageQuery {
	mtuq.order = append(mtuq.order, o...)
	return mtuq
}

// First returns the first MeshTunnelUsage entity from the query.
// Returns a *NotFoundError when no MeshTunnelUsage was found.
func (mtuq *MeshTunnelUsageQuery) First(ctx context.Context) (*MeshTunnelUsage, error) {
	nodes, err := mtuq.Limit(1).All(setContextOp(ctx, mtuq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{meshtunnelusage.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mtuq *MeshTunnelUsageQuery) FirstX(ctx context.Context) *MeshTunnelUsage {
	node, err := mtuq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MeshTunnelUsage ID from the query.
// Returns a *NotFoundError when no MeshTunnelUsage ID was found.
func (mtuq *MeshTunnelUsageQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mtuq.Limit(1).IDs(setContextOp(ctx, mtuq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{meshtunnelusage.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mtuq *MeshTunnelUsageQuery) FirstIDX(ctx context.Context) int {
	id, err := mtuq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MeshTunnelUsage entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MeshTunnelUsage entity is found.
// Returns a *NotFoundError when no MeshTunnelUsage entities are found.
func (mtuq *MeshTunnelUsageQuery) Only(ctx context.Context) (*MeshTunnelUsage, error) {
	nodes, err := mtuq.Limit(2).All(setContextOp(ctx, mtuq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{meshtunnelusage.Label}
	default:
		return nil, &NotSingularError{meshtunnelusage.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mtuq *MeshTunnelUsageQuery) OnlyX(ctx context.Context) *MeshTunnelUsage {
	node, err := mtuq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MeshTunnelUsage ID in the query.
// Returns a *NotSingularError when more than one MeshTunnelUsage ID is found.
// Returns a *NotFoundError when no entities are found.
func (mtuq *MeshTunnelUsageQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mtuq.Limit(2).IDs(setContextOp(ctx, mtuq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{meshtunnelusage.Label}
	default:
		err = &NotSingularError{meshtunnelusage.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mtuq *MeshTunnelUsageQuery) OnlyIDX(ctx context.Context) int {
	id, err := mtuq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MeshTunnelUsages.
func (mtuq *MeshTunnelUsageQuery) All(ctx context.Context) ([]*MeshTunnelUsage, error) {
	ctx = setContextOp(ctx, mtuq.ctx, ent.OpQueryAll)
	if err := mtuq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MeshTunnelUsage, *MeshTunnelUsageQuery]()
	return withInterceptors[[]*MeshTunnelUsage](ctx, mtuq, qr, mtuq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mtuq *MeshTunnelUsageQuery) AllX(ctx context.Context) []*MeshTunnelUsage {
	nodes, err := mtuq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MeshTunnelUsage IDs.
func (mtuq *MeshTunnelUsageQuery) IDs(ctx context.Context) (ids []int, err error) {
	if mtuq.ctx.Unique == nil && mtuq.path != nil {
		mtuq.Unique(true)
	}
	ctx = setContextOp(ctx, mtuq.ctx, ent.OpQueryIDs)
	if err = mtuq.Select(meshtunnelusage.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mtuq *MeshTunnelUsageQuery) IDsX(ctx context.Context) []int {
	ids, err := mtuq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mtuq *MeshTunnelUsageQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mtuq.ctx, ent.OpQueryCount)
	if err := mtuq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mtuq, querierCount[*MeshTunnelUsageQuery](), mtuq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mtuq *MeshTunnelUsageQuery) CountX(ctx context.Context) int {
	count, err := mtuq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mtuq *MeshTunnelUsageQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mtuq.ctx, ent.OpQueryExist)
	switch _, err := mtuq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mtuq *MeshTunnelUsageQuery) ExistX(ctx context.Context) bool {
	exist, err := mtuq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MeshTunnelUsageQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mtuq *MeshTunnelUsageQuery) Clone() *MeshTunnelUsageQuery {
	if mtuq == nil {
		return nil
	}
	return &MeshTunnelUsageQuery{
		config:     mtuq.config,
		ctx:        mtuq.ctx.Clone(),
		order:      append([]meshtunnelusage.OrderOption{}, mtuq.order...),
		inters:     append([]Interceptor{}, mtuq.inters...),
		predicates: append([]predicate.MeshTunnelUsage{}, mtuq.predicates...),
		// clone intermediate query.
		sql:  mtuq.sql.Clone(),
		path: mtuq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		MeshnetID int64 `json:"meshnet_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MeshTunnelUsage.Query().
//		GroupBy(meshtunnelusage.FieldMeshnetID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mtuq *MeshTunnelUsageQuery) GroupBy(field string, fields ...string) *MeshTunnelUsageGroupBy {
	mtuq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MeshTunnelUsageGroupBy{build: mtuq}
	grbuild.flds = &mtuq.ctx.Fields
	grbuild.label = meshtunnelusage.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		MeshnetID int64 `json:"meshnet_id,omitempty"`
//	}
//
//	client.MeshTunnelUsage.Query().
//		Select(meshtunnelusage.FieldMeshnetID).
//		Scan(ctx, &v)
func (mtuq *MeshTunnelUsageQuery) Select(fields ...string) *MeshTunnelUsageSelect {
	mtuq.ctx.Fields = append(mtuq.ctx.Fields, fields...)
	sbuild := &MeshTunnelUsageSelect{MeshTunnelUsageQuery: mtuq}
	sbuild.label = meshtunnelusage.Label
	sbuild.flds, sbuild.scan = &mtuq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MeshTunnelUsageSelect configured with the given aggregations.
func (mtuq *MeshTunnelUsageQuery) Aggregate(fns ...AggregateFunc) *MeshTunnelUsageSelect {
	return mtuq.Select().Aggregate(fns...)
}

func (mtuq *MeshTunnelUsageQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mtuq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mtuq); err != nil {
				return err
			}
		}
	}
	for _, f := range mtuq.ctx.Fields {
		if !meshtunnelusage.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mtuq.path != nil {
		prev, err := mtuq.path(ctx)
		if err != nil {
			return err
		}
		mtuq.sql = prev
	}
	return nil
}

func (mtuq *MeshTunnelUsageQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MeshTunnelUsage, error) {
	var (
		nodes = []*MeshTunnelUsage{}
		_spec = mtuq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MeshTunnelUsage).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MeshTunnelUsage{config: mtuq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mtuq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (mtuq *MeshTunnelUsageQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mtuq.querySpec()
	_spec.Node.Columns = mtuq.ctx.Fields
	if len(mtuq.ctx.Fields) > 0 {
		_spec.Unique = mtuq.ctx.Unique != nil && *mtuq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mtuq.driver, _spec)
}

func (mtuq *MeshTunnelUsageQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(meshtunnelusage.Table, meshtunnelusage.Columns, sqlgraph.NewFieldSpec(meshtunnelusage.FieldID, field.TypeInt))
	_spec.From = mtuq.sql
	if unique := mtuq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mtuq.path != nil {
		_spec.Unique = true
	}
	if fields := mtuq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, meshtunnelusage.FieldID)
		for i := range fields {
			if fields[i] != meshtunnelusage.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mtuq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mtuq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mtuq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mtuq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mtuq *MeshTunnelUsageQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mtuq.driver.Dialect())
	t1 := builder.Table(meshtunnelusage.Table)
	columns := mtuq.ctx.Fields
	if len(columns) == 0 {
		columns = meshtunnelusage.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mtuq.sql != nil {
		selector = mtuq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mtuq.ctx.Unique != nil && *mtuq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range mtuq.predicates {
		p(selector)
	}
	for _, p := range mtuq.order {
		p(selector)
	}
	if offset := mtuq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mtuq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MeshTunnelUsageGroupBy is the group-by builder for MeshTunnelUsage entities.
type MeshTunnelUsageGroupBy struct {
	selector
	build *MeshTunnelUsageQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mtugb *MeshTunnelUsageGroupBy) Aggregate(fns ...AggregateFunc) *MeshTunnelUsageGroupBy {
	mtugb.fns = append(mtugb.fns, fns...)
	return mtugb
}

// Scan applies the selector query and scans the result into the given value.
func (mtugb *MeshTunnelUsageGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mtugb.build.ctx, ent.OpQueryGroupBy)
	if err := mtugb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MeshTunnelUsageQuery, *MeshTunnelUsageGroupBy](ctx, mtugb.build, mtugb, mtugb.build.inters, v)
}

func (mtugb *MeshTunnelUsageGroupBy) sqlScan(ctx context.Context, root *MeshTunnelUsageQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mtugb.fns))
	for _, fn := range mtugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mtugb.flds)+len(mtugb.fns))
		for _, f := range *mtugb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mtugb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mtugb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MeshTunnelUsageSelect is the builder for selecting fields of MeshTunnelUsage entities.
type MeshTunnelUsageSelect struct {
	*MeshTunnelUsageQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mtus *MeshTunnelUsageSelect) Aggregate(fns ...AggregateFunc) *MeshTunnelUsageSelect {
	mtus.fns = append(mtus.fns, fns...)
	return mtus
}

// Scan applies the selector query and scans the result into the given value.
func (mtus *MeshTunnelUsageSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mtus.ctx, ent.OpQuerySelect)
	if err := mtus.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MeshTunnelUsageQuery, *MeshTunnelUsageSelect](ctx, mtus.MeshTunnelUsageQuery, mtus, mtus.inters, v)
}

func (mtus *MeshTunnelUsageSelect) sqlScan(ctx context.Context, root *MeshTunnelUsageQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mtus.fns))
	for _, fn := range mtus.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mtus.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mtus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

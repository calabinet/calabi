// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/calabi/calabi/apps/calabi-coord/internal/platform/store/ent/meshconnrecord"
	"github.com/calabi/calabi/apps/calabi-coord/internal/platform/store/ent/predicate"
)

// MeshConnRecordQuery is the builder for querying MeshConnRecord entities.
type MeshConnRecordQuery struct {
	config
	ctx        *QueryContext
	order      []meshconnrecord.OrderOption
	inters     []Interceptor
	predicates []predicate.MeshConnRecord
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MeshConnRecordQuery builder.
func (mcrq *MeshConnRecordQuery) Where(ps ...predicate.MeshConnRecord) *MeshConnRecordQuery {
	mcrq.predicates = append(mcrq.predicates, ps...)
	return mcrq
}

// Limit the number of records to be returned by this query.
func (mcrq *MeshConnRecordQuery) Limit(limit int) *MeshConnRecordQuery {
	mcrq.ctx.Limit = &limit
	return mcrq
}

// Offset to start from.
func (mcrq *MeshConnRecordQuery) Offset(offset int) *MeshConnRecordQuery {
	mcrq.ctx.Offset = &offset
	return mcrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mcrq *MeshConnRecordQuery) Unique(unique bool) *MeshConnRecordQuery {
	mcrq.ctx.Unique = &unique
	return mcrq
}

// Order specifies how the records should be ordered.
func (mcrq *MeshConnRecordQuery) Order(o ...meshconnrecord.OrderOption) *MeshConnRecordQuery {
	mcrq.order = append(mcrq.order, o...)
	return mcrq
}

// First returns the first MeshConnRecord entity from the query.
// Returns a *NotFoundError when no MeshConnRecord was found.
func (mcrq *MeshConnRecordQuery) First(ctx context.Context) (*MeshConnRecord, error) {
	nodes, err := mcrq.Limit(1).All(setContextOp(ctx, mcrq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{meshconnrecord.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mcrq *MeshConnRecordQuery) FirstX(ctx context.Context) *MeshConnRecord {
	node, err := mcrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MeshConnRecord ID from the query.
// Returns a *NotFoundError when no MeshConnRecord ID was found.
func (mcrq *MeshConnRecordQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mcrq.Limit(1).IDs(setContextOp(ctx, mcrq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{meshconnrecord.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mcrq *MeshConnRecordQuery) FirstIDX(ctx context.Context) int {
	id, err := mcrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MeshConnRecord entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MeshConnRecord entity is found.
// Returns a *NotFoundError when no MeshConnRecord entities are found.
func (mcrq *MeshConnRecordQuery) Only(ctx context.Context) (*MeshConnRecord, error) {
	nodes, err := mcrq.Limit(2).All(setContextOp(ctx, mcrq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{meshconnrecord.Label}
	default:
		return nil, &NotSingularError{meshconnrecord.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mcrq *MeshConnRecordQuery) OnlyX(ctx context.Context) *MeshConnRecord {
	node, err := mcrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MeshConnRecord ID in the query.
// Returns a *NotSingularError when more than one MeshConnRecord ID is found.
// Returns a *NotFoundError when no entities are found.
func (mcrq *MeshConnRecordQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mcrq.Limit(2).IDs(setContextOp(ctx, mcrq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{meshconnrecord.Label}
	default:
		err = &NotSingularError{meshconnrecord.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mcrq *MeshConnRecordQuery) OnlyIDX(ctx context.Context) int {
	id, err := mcrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MeshConnRecords.
func (mcrq *MeshConnRecordQuery) All(ctx context.Context) ([]*MeshConnRecord, error) {
	ctx = setContextOp(ctx, mcrq.ctx, ent.OpQueryAll)
	if err := mcrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MeshConnRecord, *MeshConnRecordQuery]()
	return withInterceptors[[]*MeshConnRecord](ctx, mcrq, qr, mcrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mcrq *MeshConnRecordQuery) AllX(ctx context.Context) []*MeshConnRecord {
	nodes, err := mcrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MeshConnRecord IDs.
func (mcrq *MeshConnRecordQuery) IDs(ctx context.Context) (ids []int, err error) {
	if mcrq.ctx.Unique == nil && mcrq.path != nil {
		mcrq.Unique(true)
	}
	ctx = setContextOp(ctx, mcrq.ctx, ent.OpQueryIDs)
	if err = mcrq.Select(meshconnrecord.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mcrq *MeshConnRecordQuery) IDsX(ctx context.Context) []int {
	ids, err := mcrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mcrq *MeshConnRecordQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mcrq.ctx, ent.OpQueryCount)
	if err := mcrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mcrq, querierCount[*MeshConnRecordQuery](), mcrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mcrq *MeshConnRecordQuery) CountX(ctx context.Context) int {
	count, err := mcrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mcrq *MeshConnRecordQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mcrq.ctx, ent.OpQueryExist)
	switch _, err := mcrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mcrq *MeshConnRecordQuery) ExistX(ctx context.Context) bool {
	exist, err := mcrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MeshConnRecordQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mcrq *MeshConnRecordQuery) Clone() *MeshConnRecordQuery {
	if mcrq == nil {
		return nil
	}
	return &MeshConnRecordQuery{
		config:     mcrq.config,
		ctx:        mcrq.ctx.Clone(),
		order:      append([]meshconnrecord.OrderOption{}, mcrq.order...),
		inters:     append([]Interceptor{}, mcrq.inters...),
		predicates: append([]predicate.MeshConnRecord{}, mcrq.predicates...),
		// clone intermediate query.
		sql:  mcrq.sql.Clone(),
		path: mcrq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		MeshnetID int64 `json:"meshnet_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MeshConnRecord.Query().
//		GroupBy(meshconnrecord.FieldMeshnetID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mcrq *MeshConnRecordQuery) GroupBy(field string, fields ...string) *MeshConnRecordGroupBy {
	mcrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MeshConnRecordGroupBy{build: mcrq}
	grbuild.flds = &mcrq.ctx.Fields
	grbuild.label = meshconnrecord.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		MeshnetID int64 `json:"meshnet_id,omitempty"`
//	}
//
//	client.MeshConnRecord.Query().
//		Select(meshconnrecord.FieldMeshnetID).
//		Scan(ctx, &v)
func (mcrq *MeshConnRecordQuery) Select(fields ...string) *MeshConnRecordSelect {
	mcrq.ctx.Fields = append(mcrq.ctx.Fields, fields...)
	sbuild := &MeshConnRecordSelect{MeshConnRecordQuery: mcrq}
	sbuild.label = meshconnrecord.Label
	sbuild.flds, sbuild.scan = &mcrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MeshConnRecordSelect configured with the given aggregations.
func (mcrq *MeshConnRecordQuery) Aggregate(fns ...AggregateFunc) *MeshConnRecordSelect {
	return mcrq.Select().Aggregate(fns...)
}

func (mcrq *MeshConnRecordQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mcrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mcrq); err != nil {
				return err
			}
		}
	}
	for _, f := range mcrq.ctx.Fields {
		if !meshconnrecord.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mcrq.path != nil {
		prev, err := mcrq.path(ctx)
		if err != nil {
			return err
		}
		mcrq.sql = prev
	}
	return nil
}

func (mcrq *MeshConnRecordQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MeshConnRecord, error) {
	var (
		nodes = []*MeshConnRecord{}
		_spec = mcrq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MeshConnRecord).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MeshConnRecord{config: mcrq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mcrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (mcrq *MeshConnRecordQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mcrq.querySpec()
	_spec.Node.Columns = mcrq.ctx.Fields
	if len(mcrq.ctx.Fields) > 0 {
		_spec.Unique = mcrq.ctx.Unique != nil && *mcrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mcrq.driver, _spec)
}

func (mcrq *MeshConnRecordQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(meshconnrecord.Table, meshconnrecord.Columns, sqlgraph.NewFieldSpec(meshconnrecord.FieldID, field.TypeInt))
	_spec.From = mcrq.sql
	if unique := mcrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mcrq.path != nil {
		_spec.Unique = true
	}
	if fields := mcrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, meshconnrecord.FieldID)
		for i := range fields {
			if fields[i] != meshconnrecord.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mcrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mcrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mcrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mcrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mcrq *MeshConnRecordQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mcrq.driver.Dialect())
	t1 := builder.Table(meshconnrecord.Table)
	columns := mcrq.ctx.Fields
	if len(columns) == 0 {
		columns = meshconnrecord.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mcrq.sql != nil {
		selector = mcrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mcrq.ctx.Unique != nil && *mcrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range mcrq.predicates {
		p(selector)
	}
	for _, p := range mcrq.order {
		p(selector)
	}
	if offset := mcrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mcrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MeshConnRecordGroupBy is the group-by builder for MeshConnRecord entities.
type MeshConnRecordGroupBy struct {
	selector
	build *MeshConnRecordQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mcrgb *MeshConnRecordGroupBy) Aggregate(fns ...AggregateFunc) *MeshConnRecordGroupBy {
	mcrgb.fns = append(mcrgb.fns, fns...)
	return mcrgb
}

// Scan applies the selector query and scans the result into the given value.
func (mcrgb *MeshConnRecordGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mcrgb.build.ctx, ent.OpQueryGroupBy)
	if err := mcrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MeshConnRecordQuery, *MeshConnRecordGroupBy](ctx, mcrgb.build, mcrgb, mcrgb.build.inters, v)
}

func (mcrgb *MeshConnRecordGroupBy) sqlScan(ctx context.Context, root *MeshConnRecordQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mcrgb.fns))
	for _, fn := range mcrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mcrgb.flds)+len(mcrgb.fns))
		for _, f := range *mcrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mcrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mcrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MeshConnRecordSelect is the builder for selecting fields of MeshConnRecord entities.
type MeshConnRecordSelect struct {
	*MeshConnRecordQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mcrs *MeshConnRecordSelect) Aggregate(fns ...AggregateFunc) *MeshConnRecordSelect {
	mcrs.fns = append(mcrs.fns, fns...)
	return mcrs
}

// Scan applies the selector query and scans the result into the given value.
func (mcrs *MeshConnRecordSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mcrs.ctx, ent.OpQuerySelect)
	if err := mcrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MeshConnRecordQuery, *MeshConnRecordSelect](ctx, mcrs.MeshConnRecordQuery, mcrs, mcrs.inters, v)
}

func (mcrs *MeshConnRecordSelect) sqlScan(ctx context.Context, root *MeshConnRecordQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mcrs.fns))
	for _, fn := range mcrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mcrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mcrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

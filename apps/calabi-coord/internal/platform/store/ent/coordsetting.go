// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/calabinet/calabi/apps/calabi-coord/internal/platform/store/ent/coordsetting"
)

// CoordSetting is the model entity for the CoordSetting schema.
type CoordSetting struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// setting name, e.g. conn_record_retention_days
	Key string `json:"key,omitempty"`
	// stored as text; the typed accessor in core owns the parsing and the bounds
	Value string `json:"value,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CoordSetting) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case coordsetting.FieldID:
			values[i] = new(sql.NullInt64)
		case coordsetting.FieldKey, coordsetting.FieldValue:
			values[i] = new(sql.NullString)
		case coordsetting.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CoordSetting fields.
func (cs *CoordSetting) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case coordsetting.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			cs.ID = int(value.Int64)
		case coordsetting.FieldKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field key", values[i])
			} else if value.Valid {
				cs.Key = value.String
			}
		case coordsetting.FieldValue:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field value", values[i])
			} else if value.Valid {
				cs.Value = value.String
			}
		case coordsetting.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				cs.UpdatedAt = value.Time
			}
		default:
			cs.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// GetValue returns the ent.Value that was dynamically selected and assigned to the CoordSetting.
// This includes values selected through modifiers, order, etc.
func (cs *CoordSetting) GetValue(name string) (ent.Value, error) {
	return cs.selectValues.Get(name)
}

// Update returns a builder for updating this CoordSetting.
// Note that you need to call CoordSetting.Unwrap() before calling this method if this CoordSetting
// was returned from a transaction, and the transaction was committed or rolled back.
func (cs *CoordSetting) Update() *CoordSettingUpdateOne {
	return NewCoordSettingClient(cs.config).UpdateOne(cs)
}

// Unwrap unwraps the CoordSetting entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (cs *CoordSetting) Unwrap() *CoordSetting {
	_tx, ok := cs.config.driver.(*txDriver)
	if !ok {
		panic("ent: CoordSetting is not a transactional entity")
	}
	cs.config.driver = _tx.drv
	return cs
}

// String implements the fmt.Stringer.
func (cs *CoordSetting) String() string {
	var builder strings.Builder
	builder.WriteString("CoordSetting(")
	builder.WriteString(fmt.Sprintf("id=%v, ", cs.ID))
	builder.WriteString("key=")
	builder.WriteString(cs.Key)
	builder.WriteString(", ")
	builder.WriteString("value=")
	builder.WriteString(cs.Value)
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(cs.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// CoordSettings is a parsable slice of CoordSetting.
type CoordSettings []*CoordSetting
